# 333 group
_RWS := rec(
  isRWS := true,
  ordering := "shortlex",
  tidyint := 20,
  generatorOrder := [a,b,B,A],
  inverses := [A,B,b,a],
  equations := [
    [a*a,A],
    [b*b,B],
    [B*a*B*a,A*b]
  ]
);
